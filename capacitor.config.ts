import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.avinash.passwordmanager',
  appName: 'Password Manager',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;

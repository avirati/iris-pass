import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.avinashv.iris-pass',
  appName: 'IRISPass',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
